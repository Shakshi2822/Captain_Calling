import 'package:flutter/material.dart';

class HirePeopleScreen extends StatelessWidget {
  const HirePeopleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Hire people'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('Coming Soon'),
      ),
    );
  }
}
