import 'package:flutter/material.dart';

class EarnWithUsScreen extends StatelessWidget {
  const EarnWithUsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Earn With Us'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('Coming Soon'),
      ),
    );
  }
}
